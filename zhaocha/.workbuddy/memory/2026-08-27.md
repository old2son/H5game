# 2026-08-27 工作记录

## 搭建「大家来找茬·护眼版」H5 小游戏
- 路径：`G:\MyProject\game\zhaocha\eye-find-diff\`（index.html / style.css / scenes.js / game.js）
- 技术：纯前端 + Canvas，无构建步骤，可直接 `python -m http.server` 起本地服务预览（端口 8123）。
- 玩法：5 关（读写姿势/光线环境/用眼时长/屏幕距离/户外活动），每关限时 60s，找出全部差异（每关 5 处）进入下一关；通关解锁「护眼小课堂」占位图文；全部通关生成「用眼习惯健康评分」canvas 证书（可保存 PNG）。
- 美术：当前无资源，所有场景均为 `scenes.js` 中**程序化绘制占位图**（已 node 桩测试 5 场景 × 25 差异渲染无报错）。后续替换真实图只需改 `scene.render`/`diff.paint` 为 drawImage，保持 `diffs[].bbox` 命中框与 `name` 不变即可。
- 提示/计分：提示按钮每用一次扣 8 分；单关得分 = 100×(剩余秒/60) − 提示×8；总评 = 5 关平均。localStorage 记录最高进度。
