# 2026-08-25

- 在 secha 项目从零搭建了 Vite + Vue3 + TS 的「色差挑战」小游戏（Canvas 渲染）：在 N×N 同色方块中找出颜色不同的那一个，点中升级（网格变大/色差变小/时间变短），点错或超时则结束。入口：index.html / vite.config.ts / tsconfig*.json / src/main.ts / src/App.vue / src/style.css。
- dev 命令：`npm run dev`（vite，端口 5173，已 host 开启）。游戏逻辑在 src/App.vue，使用 Canvas 绘制 + pointerdown 命中检测 + requestAnimationFrame 倒计时，适配 HiDPI 与窗口缩放，最佳分存 localStorage。
- ⚠️ 重要：本沙箱中 `npm install` 在沙箱隔离下会卡住（网络出口被拦，16 分钟 0 包）；需以非沙箱方式运行（验证：加 `--no-audit --no-fund --loglevel=http` 后约 40s 装好 232 包）。后续在此项目装依赖请走非沙箱通道。

## 后续迭代（同日）
- dev 地址 `http://localhost:5173/secha/`（`vite.config.ts` 的 `base: '/secha/'`）。后台 dev server 闲置会被系统回收，残留进程仍占 5173 并服务旧代码；清理用 `netstat -ano | grep :5173` 查 PID + `taskkill /PID <id> /F`，再重启。
- 已加功能（src/App.vue）：结束页科普卡+随机抽卡、色觉灵敏度评分（sensitivity/sensGrade/sensDesc，SENS_MAX=16，bestSens 存 localStorage）、音效震动（Web Audio + navigator.vibrate，含 🔊 静音开关）、顶部「灵敏度最佳」常驻、科普卡「换一张/收起」。
- 本轮新增：**连击加分**（combo/maxCombo/comboBonus，第 2 连起每连 +3 分，棋盘顶部浮动「🔥 N 连击」横幅，gameOver 清零保留 maxCombo）；**成绩分享海报**（`sharePoster()` 离屏 canvas 绘制 720×1080 PNG，toDataURL+<a download> 下载，无第三方库）；结束弹窗加「最高连击」行与「保存成绩卡」ghost 按钮。验证 `vue-tsc -b` exit 0、5173 返回 200。
